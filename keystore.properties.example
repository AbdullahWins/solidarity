# Copy this file to keystore.properties (gitignored) at the project root,
# fill in the values from `eas credentials`, and place the downloaded .jks
# file at the project root too (path below is relative to android/app/).
storeFile=../../release.jks
storePassword=
keyAlias=
keyPassword=
